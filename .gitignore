# --- Node / Package managers ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# Keep your lockfile! (do NOT ignore package-lock.json / yarn.lock / pnpm-lock.yaml)

# --- Logs & runtime artifacts ---
logs/
*.log
*.log.*
pids/
*.pid
*.pid.lock
*.seed

# --- Coverage ---
coverage/
.nyc_output/

# --- Caches / temp ---
.cache/
.tmp/
tmp/
temp/

# --- Builds (if any) ---
dist/
build/

# --- Env & secrets ---
.env
.env.*
!.env.example   # keep a template if you have one

# --- OS cruft ---
.DS_Store
Thumbs.db

# --- Editors/IDE ---
.vscode/
.idea/
*.iml
.history/

# --- TypeScript (if introduced later) ---
*.tsbuildinfo

# --- Local data files ---
# Avoid committing test data. Your code already creates an empty file if missing.
data/recipes.json
